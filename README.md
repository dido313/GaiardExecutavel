# GaiardExecutavel

Repositório de **distribuição do cliente** (não é o código-fonte).

- `manifest.json` — versão e URLs dos zips (lido pelo launcher)
- **Releases** — `gaiard-full.zip` e `patch.zip`

Código-fonte: repositório privado/desenvolvimento separado.
